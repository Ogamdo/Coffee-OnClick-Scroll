# === 안드로이드 스튜디오 관련 파일 ===
*.iml                           # IntelliJ 프로젝트 파일
.gradle                         # Gradle 캐시 폴더
/local.properties               # 로컬 환경(Android SDK 경로 등) 설정 파일
/.idea/*                        # 안드로이드 스튜디오 프로젝트 설정 파일
!/.idea/runConfigurations/      # 실행 설정 파일은 공유 가능하도록 제외하지 않음
.DS_Store                       # macOS 메타데이터 파일
Thumbs.db                       # Windows 탐색기 캐시 파일
*.log                           # 일반적인 로그 파일 제외

# === 빌드 출력 파일 ===
/build                          # 빌드 출력 폴더
/captures                       # 디버깅 캡처 파일
.externalNativeBuild            # 네이티브 빌드 캐시 폴더
.cxx                            # C++ 빌드 시스템 출력 파일

# === 기타 ===
local.properties                # 로컬 환경 파일 (SDK 경로 등, 버전 관리 제외)

# === macOS 및 Windows 관련 ===
.DS_Store                       # macOS 메타데이터 파일
.AppleDouble                    # macOS 리소스 포크 파일
.LSOverride                     # macOS Finder 설정
Thumbs.db                       # Windows 탐색기에서 생성되는 캐시 파일
Desktop.ini                     # Windows 탐색기 설정 파일



generated by GPT( Android Dev Assist)
prompt:"교수님께 제출할 과제임을 염두에 두고 .gitignore를 작성해줘"
